# Qdrant REST endpoint
QDRANT_URL=https://qdrant.swrks.sh

# Ollama endpoint (Spark)
OLLAMA_URL=https://spark.swrks.sh/ollama

# Root directory for state, projects output
CLAWRTEX_ROOT=/Users/clawdio/clawrtex

# Credentials file (defaults to ./credentials.json relative to pipeline root)
# CLAWRTEX_CREDENTIALS=./credentials.json
