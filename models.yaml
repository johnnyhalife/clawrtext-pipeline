# Model assignments per pipeline stage.
# Edit here — no .ts changes needed.
# Env vars override these when set.

map-decks: nemotron3:33b        # vision model — reads slide images
embed: qwen3-embedding:8b       # embeddings
reduce: qwen3.6:35b             # per-deck summarization
compiled-truth: qwen3.6:35b     # incremental compiled truth synthesis
